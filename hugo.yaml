baseURL: "https://haostudy.me"
languageCode: "ru"
title: "Хаостади – обучение и бизнес в Китае"

disableKinds:
  - taxonomy

outputs:
  home:
    - HTML
  section:
    - HTML

markup:
  goldmark:
    renderer:
      unsafe: true
    parser:
      wrapStandAloneImageWithinParagraph: false

build:
  writeStats: true